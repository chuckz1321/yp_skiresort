
eureka:
  client:
    serviceUrl:
      defaultZone: http://ypserver.mybluemix.net/eureka
  instance:
    hostname: localhost
    metadataMap:
      instanceId: yp_skiresort

spring:
  application:
    name: yp_skiresort
server:
  port: 8762

---
spring:
  profiles: cloud
eureka:
  instance:
    nonSecurePort: 80